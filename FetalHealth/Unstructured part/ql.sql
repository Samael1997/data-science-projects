
            SELECT id_doc,t_constipation, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_constipation as decimal)>0
            ORDER BY t_constipation DESC;
            


            SELECT id_doc,t_bloating, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_bloating as decimal)>0
            ORDER BY t_bloating DESC;
            


            SELECT id_doc,t_bleeding, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_bleeding as decimal)>0
            ORDER BY t_bleeding DESC;
            


            SELECT id_doc,t_cramps, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_cramps as decimal)>0
            ORDER BY t_cramps DESC;
            


            SELECT id_doc,t_pain, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_pain as decimal)>0
            ORDER BY t_pain DESC;
            


            SELECT id_doc,t_myths, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_myths as decimal)>0
            ORDER BY t_myths DESC;
            


            SELECT id_doc,t_toxoplasmosis, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_toxoplasmosis as decimal)>0
            ORDER BY t_toxoplasmosis DESC;
            


            SELECT id_doc,t_miscarriage, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_miscarriage as decimal)>0
            ORDER BY t_miscarriage DESC;
            


            SELECT id_doc,t_flutters, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_flutters as decimal)>0
            ORDER BY t_flutters DESC;
            


            SELECT id_doc,t_labor, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_labor as decimal)>0
            ORDER BY t_labor DESC;
            


            SELECT id_doc,t_nausea, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_nausea as decimal)>0
            ORDER BY t_nausea DESC;
            


            SELECT id_doc,t_heartburn, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_heartburn as decimal)>0
            ORDER BY t_heartburn DESC;
            


            SELECT id_doc,t_tiredness, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_tiredness as decimal)>0
            ORDER BY t_tiredness DESC;
            


            SELECT id_doc,t_cancers, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_cancers as decimal)>0
            ORDER BY t_cancers DESC;
            


            SELECT id_doc,t_preeclampsia, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_preeclampsia as decimal)>0
            ORDER BY t_preeclampsia DESC;
            


            SELECT id_doc,t_skydiving, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_skydiving as decimal)>0
            ORDER BY t_skydiving DESC;
            


            SELECT id_doc,t_death, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_death as decimal)>0
            ORDER BY t_death DESC;
            


            SELECT id_doc,t_fatigue, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_fatigue as decimal)>0
            ORDER BY t_fatigue DESC;
            


            SELECT id_doc,t_panic, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_panic as decimal)>0
            ORDER BY t_panic DESC;
            


            SELECT id_doc,t_afterbirth, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_afterbirth as decimal)>0
            ORDER BY t_afterbirth DESC;
            


            SELECT id_doc,t_hypertension, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_hypertension as decimal)>0
            ORDER BY t_hypertension DESC;
            


            SELECT id_doc,t_backache, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_backache as decimal)>0
            ORDER BY t_backache DESC;
            


            SELECT id_doc,t_vitamins, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_vitamins as decimal)>0
            ORDER BY t_vitamins DESC;
            


            SELECT id_doc,t_minerals, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_minerals as decimal)>0
            ORDER BY t_minerals DESC;
            


            SELECT id_doc,t_caffeine, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_caffeine as decimal)>0
            ORDER BY t_caffeine DESC;
            


            SELECT id_doc,t_chocolate, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_chocolate as decimal)>0
            ORDER BY t_chocolate DESC;
            


            SELECT id_doc,t_alcohol, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_alcohol as decimal)>0
            ORDER BY t_alcohol DESC;
            


            SELECT id_doc,t_iron, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_iron as decimal)>0
            ORDER BY t_iron DESC;
            


            SELECT id_doc,t_zinc, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_zinc as decimal)>0
            ORDER BY t_zinc DESC;
            


            SELECT id_doc,t_silicone, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_silicone as decimal)>0
            ORDER BY t_silicone DESC;
            


            SELECT id_doc,t_tea, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_tea as decimal)>0
            ORDER BY t_tea DESC;
            


            SELECT id_doc,t_relaxin, content
            FROM doc JOIN unstructured_doc on doc.id_doc = unstructured_doc.doc_id
            WHERE CAST(t_relaxin as decimal)>0
            ORDER BY t_relaxin DESC;
            


